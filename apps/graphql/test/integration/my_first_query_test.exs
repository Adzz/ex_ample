defmodule MyFirstQueryTest do
  use GraphqlWeb.ConnCase

  describe "queries for the field successfully" do
    test "Queries for Average STC (in days) successfully" do
      # seed database
    end
  end
end
